# This file simulates a local development .env that gets
# accidentally copied into the image because there is no .dockerignore
API_KEY=dev-api-key-local-testing
DATABASE_URL=postgresql://localuser:localpassword@localhost:5432/devdb
STRIPE_SECRET=sk_test_localdevkey123456789
