# Internal Financial Reporting Application
# Security remediation applied — dependency review date: current
# Reviewed by: Security Engineering Team

# Web framework — updated to fix CVE-2023-30861 (session cookie leak)
Flask==3.0.3

# HTTP requests — updated to fix CVE-2023-32681 (proxy header injection)
requests==2.31.0

# Cryptography — updated to fix multiple CVEs including RSA decryption issues
cryptography==42.0.8

# YAML parsing — updated to fix CVE-2022-1471 (arbitrary code execution)
PyYAML==6.0.1

# XML processing — updated to fix CVE-2022-2309, null byte injection
lxml==4.9.3

# Image processing — updated to fix CVE-2023-44271 (DoS via crafted image)
Pillow==10.3.0

# Database ORM — updated to fix CVE-2022-0696
SQLAlchemy==2.0.30

# JWT — CRITICAL: 1.x had no algorithm enforcement, completely rewritten in 2.x
PyJWT==2.8.0

# Template engine — updated to fix CVE-2024-34064
Jinja2==3.1.4
